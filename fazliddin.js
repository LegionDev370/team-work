// 1.
// `Git — versiya nazorati tizimi, koddagi o'zgarishlarni saqlash va boshqarish uchun ishlatiladi.`;

// 2.
// `GitHub — Git repositorylarni internetda saqlash xizmati.
//  Git — lokal tool, GitHub — online platforma.`;

// 3.
// `Commit — o'zgarishlarni saqlash. Maqsad: tarixni yozib borish.`;

// 4.
// `Staging area — commit oldidan fayllarni tanlash joyi.`;

// 5.
// `git add — stagingga qo'shadi.
// git commit — saqlaydi.`;

// 6.
// `Branch — alohida ish yo'nalishi. Xatolarsiz ishlash uchun.`;

// 7.
// `git branch name, git checkout name`;

// 8.
// `Merge — branchlarni birlashtirish.`;

// 9.
// `Agar tarix to'g'ri ketayotgan bo'lsa, oddiy qo'shilish.`;

// 10.
// `Fast-forward — yangi commit yo'q.
// Merge commit — yangi commit yaratiladi.`;

// 11.
// `Kod o'zgarishini tekshirib, qo'shishni so'rash.`;

// 12.
// `Katta o'zgarishlarda — kerak.
// Kichik yoki o'zingiz ishlasangiz — shart emas.`;

// 13.
// `push — serverga yuboradi.
// pull — serverdan yuklaydi.`;

// 14.
// `fetch — faqat yuklaydi.
// pull — yuklab, birlashtiradi.`;

// 15.
// `Qisqa commit tarixini ko'rsatadi.`;

// 16.
// `Commitni bekor qiladi.`;

// 17.
// `reset --hard — o'chiradi.
// revert — xavfsiz bekor qiladi.`;

// 18.
// `clone → branch → code → add → commit → push → PR → merge`;

// 19.
// `Feature branchni hozirgi branchga qo'shadi.
// Ba'zan merge commit yaratiladi.`;

// 20.
// `Har bir commitning noyob ID raqami.`;
